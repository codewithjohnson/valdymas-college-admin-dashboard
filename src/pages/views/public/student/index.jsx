import React, { memo, useState } from "react";
import { useParams, useNavigate, useLocation } from "react-router-dom";

const Student = memo(() => {
  return <div>Student with an id</div>;
});
export default Student;
